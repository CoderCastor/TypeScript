"use strict";
// type name = string;
// type name = number;     ---> throws error due to same name
function getUser(user) {
    return user.name.toUpperCase();
}
let tushar = {
    name: "rakesh",
    color: "black",
    height: 12
};
console.log(getUser(tushar));
function getFood(dish) {
    dish.name;
    dish.price;
}
function gettFood(dishes) {
    dishes.name;
    dishes.price;
    dishes.mitha;
}
